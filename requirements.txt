# Core LangChain
langchain>=0.2.0
langchain-openai>=0.1.0
langchain-core>=0.2.0
langchain-community>=0.2.0

# Vector Store — FAISS (dibutuhkan oleh utils/vectorstore.py)
faiss-cpu>=1.7.0

# Tokenizer (dibutuhkan oleh OpenAI embeddings)
tiktoken>=0.5.0

# Environment variables (dibutuhkan oleh config.py)
python-dotenv>=1.0.0

# OpenAI SDK
openai>=1.0.0

# Cloud storage (dibutuhkan oleh agents/cloud.py — Cloudflare R2 via S3-compatible API)
boto3>=1.28.0